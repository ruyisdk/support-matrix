{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "plugin_schema/doc_only.schema.json",
    "title": "Schema using doc_only plugin",
    "description": "doc_only plugin does nothing but mark the system as doc_only",
    "properties": {
        "vendor": {
            "type": "string",
            "description": "The vendor of the system which this config represents"
        },
        "system": {
            "type": "string",
            "description": "The system's id of the system which this config represents"
        },
        "variant": {
            "anyOf": [
                {
                    "type": "string"
                },                 {
                    "type": "null"
                }
            ],
            "description": "The variant of the system which this config represents\n'null' if is 'null'"
        }
    },
    "required": [
        "vendor",
        "system",
        "variant"
    ]
}